{
  "name": "CampusTrafic Dev Container",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "lts"
    },
    "ghcr.io/devcontainers-extra/features/uv:1": {}
  },
  "postCreateCommand": "bash -lc 'set -euo pipefail; if [ -f pyproject.toml ]; then uv sync; fi; if [ -f web/package.json ]; then cd web; npm install; if npm pkg get devDependencies.vite >/dev/null 2>&1; then npm run build; fi; fi'",
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": []
    }
  }
}

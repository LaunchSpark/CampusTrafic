# Web Training (`web/training/`)

Training dashboard frontend for retrain operations.

## Capabilities

- View training stages and progress
- Stream logs (SSE/WebSocket-backed endpoints)
- Inspect live metrics
- Compare completed runs

Uses `/train/*` endpoints exposed by FastAPI.

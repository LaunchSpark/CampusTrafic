# Web Admin (`web/admin/`)

Admin map-builder frontend for world draft workflows.

## Capabilities

- List/create/clone drafts
- Edit nodes, edges, and coordinates
- Adjust routing-relevant attributes
- Publish draft as a new world version

Uses `/world/drafts*` API endpoints.

# Web Visualization (`web/viz/`)

Visualization modules for field and world rendering.

## Responsibilities

- Render vector overlays from precomputed tiles
- Render density heatmaps
- Render world graph (nodes/edges/grid context)
- Sync visual state with run/time/filter controls

Focus on fast playback from static tile/data sources.
